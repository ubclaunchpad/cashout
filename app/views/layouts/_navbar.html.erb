<nav class="navbar navbar-default">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <%= link_to 'Cashout 💸', home_index_path, :class => "navbar-brand" %>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse">
            <% if user_signed_in? %>
            <ul class="nav navbar-nav navbar-left">
                <li><%= link_to 'Purchases', purchases_path %></li>
                <li><%= link_to 'Portfolio', portfolio_path %></li>
                <li><%= link_to 'Records', snapshot_path %></li>
                <li><%= link_to 'Assets Difference', difference_path %></li>
            </ul>
            <% end %>
            <ul class="nav navbar-nav navbar-right">
                <!-- This will link to Devise (for users) pages -->
                <% if user_signed_in? %>
                <li> <%= link_to 'Edit User', edit_user_registration_path %> </li>
                <li> <%= link_to 'Sign Out', destroy_user_session_path, :method => :delete %> </li>
                <% else %>
                <li> <%= link_to 'Sign Up', new_user_registration_path %> </li>
                <li> <%= link_to 'Sign In', new_user_session_path %> </li>
                <% end %>
            </ul>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>
