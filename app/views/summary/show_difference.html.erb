<%= link_to 'Portfolio', portfolio_path, :class => "btn btn-default" %>
<%= link_to 'Assets Difference', difference_path, :class => "btn btn-default" %>


<h1>Difference</h1>

<table class="table">
    <thead>
        <tr>
            <th>Currency</th>
            <th>Value</th>
            <th colspan=3></th>
        </tr>
    </thead>

    <tbody>
        <% @difference_data.each do |currency, value| %>
        <tr>
            <td><%= currency %></td>
            <td><%= value.round(2) %></td>
        </tr>
        <% end %>
    </tbody>
</table>
